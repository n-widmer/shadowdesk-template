---
description: "Build a new skill end-to-end or extend an existing one: routes through grill-me, locks the spec, enforces prerequisites, drafts SKILL.md, tests it, and commits. Type it when you have a recurring task you want automated."
disable-model-invocation: true
argument-hint: "[what to build]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

# skill-builder

*Provided as part of your ShadowDesk engagement. Not for resale or redistribution.*

Read the full workflow at `${CLAUDE_PLUGIN_ROOT}/skills/skill-builder/SKILL.md` and execute it exactly. Any sub-files it references (e.g. `references/aios-appends.md`) live under `${CLAUDE_PLUGIN_ROOT}/skills/skill-builder/` — read them by that path.

Input: $ARGUMENTS
