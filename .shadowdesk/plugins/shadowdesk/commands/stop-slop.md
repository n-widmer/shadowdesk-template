---
description: "Strip predictable AI tells out of prose (throat-clearing openers, jargon, binary contrasts, false agency, passive voice, em dashes) so it reads like a human wrote it. Type it on any draft you will publish or send."
argument-hint: "[the text to de-slop]"
disable-model-invocation: true
---

# stop-slop

*Provided as part of your ShadowDesk engagement. Not for resale or redistribution.*

Read the full workflow at `${CLAUDE_PLUGIN_ROOT}/skills/stop-slop/SKILL.md` and execute it exactly on the text in `$ARGUMENTS`. Any files it points to (e.g. `references/`) live under `${CLAUDE_PLUGIN_ROOT}/skills/stop-slop/` — read them by that absolute path.

Input: $ARGUMENTS
